# Nixpacks configuration for Railway
# This file helps Railway understand how to build your application

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma migrate deploy"]

[start]
cmd = "npm start"